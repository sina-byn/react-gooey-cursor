// ! mock shadcn component to avoid registry build errors
// ! later replace with the actual shadcn component
// ! visit `registry.json` for more

export function Mock() {
  return <div>Mock</div>;
}
